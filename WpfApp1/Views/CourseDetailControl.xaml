<UserControl x:Class="ClassroomAssignment.Views.CourseDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ClassroomAssignment.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <Label Content="Section Information" FontSize="15" Grid.Row="0" BorderBrush="Black" BorderThickness="0,0,0,2" Margin="0,0,0,15"/>


        <Grid Height="Auto" Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">

                <StackPanel Orientation="Horizontal">
                    <Label Content="Title/Topic"   />
                    <TextBox Text="{Binding CourseTitle}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Section #"/>
                    <TextBox Text="{Binding SectionNumber}"  BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Credit Hrs"/>
                    <TextBox Text="{Binding CreditHrs}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Status"/>
                    <TextBox Text="{Binding Status}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Grade Mode"/>
                    <TextBox Text="{Binding GradeMode}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1">

                <StackPanel Orientation="Horizontal">
                    <Label Content="Session"/>
                    <TextBox Text="{Binding Session}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Campus"/>
                    <TextBox Text="{Binding Campus}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Instr. Method"/>
                    <TextBox Text="{Binding InstructionMethod}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Section Type"/>
                    <TextBox Text="{Binding SectionType}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>

                <StackPanel Orientation="Horizontal">
                    <Label Content="Grade Mode"/>
                    <TextBox Text="{Binding GradeMode}" BorderBrush="Gray" BorderThickness="0,0,0,1"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Label Content="Instructor" FontWeight="Bold"/>
                <TextBox Text="{Binding Instructor}"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label Content="Room" FontWeight="Bold"/>
                <TextBox Text="{Binding Room}"/>
            </StackPanel>

            <StackPanel Grid.Column="2">
                <Label Content="Schedule" FontWeight="Bold"/>
                <TextBox Text="{Binding MeetingPattern}"/>
            </StackPanel>
        </Grid>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0">
                <Label Content="Enrollment" FontWeight="Bold"/>
                <StackPanel Orientation="Horizontal">
                    <StackPanel Orientation="Vertical">
                        <Label Content="Maximum"/>
                        <TextBox Text="{Binding MaximumEnrollment}"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical">
                        <Label Content="Wait Cap"/>
                        <TextBox Text="{Binding WaitCap}"/>
                    </StackPanel>

                    <StackPanel Orientation="Vertical">
                        <Label Content="Rm Cap Request"/>
                        <TextBox Text="{Binding RoomCapRequest}"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <Label Content="Notes" FontWeight="Bold"/>
                <TextBox Text="{Binding Notes}"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
